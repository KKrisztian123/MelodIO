import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.melodio.app',
  appName: 'MelodIO',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
